namespace LoginApp
{
    public class LoginManager
    {
        public bool Login(User user)
        {
            //TODO check e.g. in database if user exists
            return true;
        }
    }
}
